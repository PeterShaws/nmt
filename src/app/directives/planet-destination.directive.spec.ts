import { PlanetDestinationDirective } from './planet-destination.directive';

describe('PlanetDestinationDirective', () => {
  it('should create an instance', () => {
    const directive = new PlanetDestinationDirective();
    expect(directive).toBeTruthy();
  });
});
