import { PortalAddressDirective } from './portal-address.directive';

describe('PortalAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalAddressDirective();
    expect(directive).toBeTruthy();
  });
});
